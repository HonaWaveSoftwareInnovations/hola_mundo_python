pipeline {
    agent any

    stages {
        stage('Ejecutar aplicación Python') {
            steps {
                echo 'Ejecutando el archivo main.py...'
                bat 'python main.py'
            }
        }
    }
}
